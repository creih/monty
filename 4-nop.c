#include "monty.h"
/**
 * nop - does nothing doesn't even terminate
 * @stack: points to stack
 * @line_number: line to print at stderr
 * Return: None
 */
void nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
